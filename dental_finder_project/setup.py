from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='perform a data analysis using a dataset from Real, Inc., which contains information on the acquisition of clinics.',
    author='vfamim',
    license='',
)
